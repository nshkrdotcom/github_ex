defmodule GitHubEx do
  @moduledoc """
  Thin, generated Elixir SDK for the GitHub REST API.

  The generated endpoint wrappers stay close to the upstream GitHub REST OpenAPI
  description, while `GitHubEx.Client` owns runtime defaults such as headers,
  retries, response wrapping, and auth helpers.
  """
end
